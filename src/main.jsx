import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
```

4. **"Commit changes"**

### Fichier 5 : `src/App.jsx`

1. **"Add file"** → **"Create new file"**
2. Nom : `src/App.jsx`
3. Colle **tout le contenu** du fichier `vigie-factures-app.jsx` que tu as téléchargé tout à l'heure. Ouvre-le avec le Bloc-notes, copie tout, et colle.
4. **"Commit changes"**

---

📸 Quand c'est fait, montre-moi ton repo GitHub ! Il devrait avoir cette structure :
```
vigie-factures/
  README.md
  package.json
  vite.config.js
  index.html
  src/
    main.jsx
    App.jsx
